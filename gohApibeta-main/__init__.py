try:
    # Например, главный класс в gohApibeta/gptclient.py
    from .gptclient import GPTClient
except ImportError as e:
    # Если библиотека установлена, но что-то не так с импортом,
    # выдаём понятное сообщение
    raise ImportError(
        "Ошибка импорта gohApibeta: не удалось загрузить внутренние модули.\n"
        "Проверьте, что пакет установлен корректно и версия Python совпадает с той, для которой пакет собран.\n"
        "Если вы в Termux, запускайте скрипты именно из домашней директории (например, ~/), а не /sdcard.\n"
        f"Исходная ошибка: {e}"
    ) from e